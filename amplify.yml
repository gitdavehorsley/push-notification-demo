version: 1
backend:
  phases:
    build:
      commands:
        - cd amplify/backend/function/pushnotificationdemo1e717369/src && npm install
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "No build commands needed"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
